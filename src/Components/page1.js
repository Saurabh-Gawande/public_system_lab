import React from "react";

function page1() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >the main page </div>
  );
}

export default page1;
